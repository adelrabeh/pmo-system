import React from "react"; import { NavLink } from "react-router-dom"; import { logout } from "../services/auth";
export default function AppLayout({children}){ return <div className="layout" dir="rtl"><aside className="sidebar"><h2>PMO</h2><NavLink to="/" className={({isActive})=>isActive?'active':''}>لوحة المحفظة</NavLink><NavLink to="/projects" className={({isActive})=>isActive?'active':''}>المشاريع</NavLink><NavLink to="/projects/new" className={({isActive})=>isActive?'active':''}>إنشاء مشروع</NavLink><button className="btn btn-danger" onClick={logout}>تسجيل الخروج</button></aside><div className="content"><div className="topbar"><strong>منصة PMO الذكية</strong></div><div className="container">{children}</div></div></div> }
