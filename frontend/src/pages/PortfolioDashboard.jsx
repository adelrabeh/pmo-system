import React,{useEffect,useState} from "react"; import { getProjects } from "../services/projects";
export default function PortfolioDashboard(){ const [items,setItems]=useState([]); useEffect(()=>{getProjects().then(r=>setItems(r.data.results||r.data));},[]); return <div><h1>لوحة المحفظة</h1><div className="grid grid-4"> <div className="card">إجمالي المشاريع: {items.length}</div><div className="card">المخاطر: {items.reduce((a,b)=>a+(b.risks?.length||0),0)}</div><div className="card">التحديات: {items.reduce((a,b)=>a+(b.issues?.length||0),0)}</div><div className="card">طلبات التغيير: {items.reduce((a,b)=>a+(b.change_requests?.length||0),0)}</div></div></div>; }
