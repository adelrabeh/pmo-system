import React,{useEffect,useState} from "react"; import { useParams } from "react-router-dom"; import { getProject } from "../services/projects";
export default function ProjectDetails(){ const {id}=useParams(); const [p,setP]=useState(null); useEffect(()=>{getProject(id).then(r=>setP(r.data));},[id]); if(!p) return <div>جاري التحميل...</div>; return <div><h1>{p.project_name_ar}</h1><div className="grid grid-4"><div className="card">الحالة: {p.status_name}</div><div className="card">المرحلة: {p.phase_name}</div><div className="card">الإنجاز: {p.completion_percentage}%</div><div className="card">خط الأساس: {p.baseline_exists ? 'نعم':'لا'}</div></div><h2>المخاطر</h2><ul>{(p.risks||[]).map(r=><li key={r.id}>{r.title}</li>)}</ul></div>; }
